---
# Gateway API CRDs
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: gateway-api-crds
  namespace: flux-system
spec:
  interval: 1h
  path: ./kubernetes/fluxcd/infra/base/api-support/gateway
  prune: false
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system

---
# Traefik controller
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: traefik-controller
  namespace: flux-system
spec:
  interval: 10m
  path: ./kubernetes/fluxcd/infra/base/controllers/traefik
  prune: true
  wait: true
  dependsOn:
    - name: gateway-api-crds
  sourceRef:
    kind: GitRepository
    name: flux-system

---
# Traefik configs
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: traefik-configs
  namespace: flux-system
spec:
  interval: 10m
  path: ./kubernetes/fluxcd/infra/base/configs/gateway
  prune: true
  wait: true
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-config
        optional: false
  dependsOn:
    - name: gateway-api-crds
  sourceRef:
    kind: GitRepository
    name: flux-system